<!--
    PlaylistView —— Phase 2 队列 UI 控件 (Phase 12: 紧凑表格优化)。
    布局: 2 行 Grid
      Row 0 (Auto) : 工具栏（添加/清空/导入文件夹 + 刷新）
      Row 1 (*)    : ListBox（自定义模板含表头行 + ScrollViewer，保证列对齐）

    DataContext = PlaylistViewModel（Phase 6 由 MainWindow.xaml 绑定 Playlists.ViewedPlaylist）
-->
<UserControl x:Class="UmaPlayer.Views.Controls.PlaylistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:UmaPlayer.Converters"
             xmlns:local="clr-namespace:UmaPlayer.Views.Controls">
    <UserControl.Resources>
        <converters:TimeSpanToStringConverter x:Key="TimeSpanToString"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>

    <Border AllowDrop="True"
            Background="Transparent"
            DragEnter="Root_DragEnter"
            DragOver="Root_DragOver"
            DragLeave="Root_DragLeave"
            Drop="Root_Drop">
        <Grid Margin="16,8,16,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: 工具栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <!-- 左侧：添加 / 清空 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Command="{Binding AddToQueueCommand}" Padding="12,4">
                    <TextBlock Text="+ 添加" FontSize="12"/>
                </Button>
                <Button Padding="12,4" Margin="8,0,0,0"
                        Command="{Binding DataContext.Playlists.ImportFolderCommand,
                                 RelativeSource={RelativeSource AncestorType=Window}}">
                    <TextBlock Text="📂 导入文件夹" FontSize="12"/>
                </Button>
                <Button Command="{Binding ClearQueueCommand}" Padding="12,4" Margin="8,0,0,0">
                    <TextBlock Text="清空" FontSize="12"/>
                </Button>
            </StackPanel>

            <!-- 右侧：刷新按钮（仅文件夹绑定歌单可见） -->
            <Button Width="32" Height="32"
                    Padding="0" HorizontalAlignment="Right"
                    Background="Transparent" BorderThickness="0"
                    ToolTip="刷新文件夹"
                    Visibility="{Binding HasSourceFolder, Converter={StaticResource BoolToVisibility}}"
                    Command="{Binding DataContext.Playlists.RefreshPlaylistCommand,
                             RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="{Binding}">
                <TextBlock Text="🔄" FontSize="14"
                           FontFamily="Segoe UI Emoji"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </Grid>


        <!-- Row 1: 队列列表 -->
        <Border x:Name="QueueListBorder"
                Grid.Row="1" Background="{StaticResource BackgroundSecondary}"
                CornerRadius="4" BorderThickness="2">
            <!-- BorderBrush 必须由 Style.Setter 提供默认值；写成 local 属性会以 local-value
                 优先级压过 Style.Triggers 里的 Setter，导致 IsDragOver=True 高亮失效。
                 高亮范围：仅圆角矩形列表框，不含上方工具栏（外层 Border 仅承载 AllowDrop）。 -->
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="local:DragDropExtensions.IsDragOver" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource AccentPrimary}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ListBox x:Name="QueueList"
                     ItemsSource="{Binding SortedView}"
                     SelectedItem="{Binding SelectedTrack, Mode=TwoWay}"
                     SelectionMode="Extended"
                     AllowDrop="True"
                     Background="Transparent" BorderThickness="0"
                     Foreground="{StaticResource ForegroundPrimary}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="QueueList_MouseDoubleClick"
                     KeyDown="QueueList_KeyDown"
                     PreviewMouseLeftButtonDown="QueueList_PreviewMouseLeftButtonDown"
                     PreviewMouseMove="QueueList_PreviewMouseMove"
                     PreviewMouseLeftButtonUp="QueueList_PreviewMouseLeftButtonUp"
                     DragOver="QueueList_DragOver"
                     DragLeave="QueueList_DragLeave"
                     Drop="QueueList_Drop">
                <!-- 覆盖模板：表头行 + ScrollViewer 共享同一容器宽度，保证列对齐 -->
                <ListBox.Template>
                    <ControlTemplate TargetType="ListBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <!-- 表头行：列定义与 DataTemplate 一致 -->
                            <Grid Grid.Row="0" Margin="8,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="2.5*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="20"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource CaptionText}"/>
                                <TextBlock Grid.Column="1" Text="#" Style="{StaticResource CaptionText}" HorizontalAlignment="Right"/>
                                <Button Grid.Column="2" Content="标题" Click="Header_Click" Tag="Title"
                                        Style="{StaticResource {x:Type Button}}" Padding="0"
                                        HorizontalContentAlignment="Center" Cursor="Hand"/>
                                <Button Grid.Column="3" Content="艺术家" Click="Header_Click" Tag="Artist"
                                        Style="{StaticResource {x:Type Button}}" Padding="0"
                                        HorizontalContentAlignment="Center" Cursor="Hand"/>
                                <Button Grid.Column="4" Content="专辑" Click="Header_Click" Tag="Album"
                                        Style="{StaticResource {x:Type Button}}" Padding="0"
                                        HorizontalContentAlignment="Center" Cursor="Hand"/>
                                <Button Grid.Column="5" Content="时长" Click="Header_Click" Tag="Duration"
                                        Style="{StaticResource {x:Type Button}}" Padding="0"
                                        HorizontalContentAlignment="Right" Cursor="Hand"/>
                                <TextBlock Grid.Column="6" Style="{StaticResource CaptionText}"/>
                            </Grid>
                            <ScrollViewer Grid.Row="1"
                                          HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto"
                                          CanContentScroll="True">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Grid>
                    </ControlTemplate>
                </ListBox.Template>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Grid>
                                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                                Padding="8,4">
                                            <ContentPresenter/>
                                        </Border>
                                        <!-- 底部分隔线 -->
                                        <Border Height="1" Background="{StaticResource BackgroundTertiary}"
                                                VerticalAlignment="Bottom" Margin="8,0"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Background"
                                                    Value="{StaticResource BackgroundTertiary}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="bd" Property="Background"
                                                    Value="{StaticResource AccentPressed}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="2.5*"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>
                            <!-- ▶ 标记: 由 code-behind 在 CurrentIndex 变化时切换 -->
                            <TextBlock x:Name="PART_Marker" Grid.Column="0" Text="" FontSize="11"
                                       Foreground="{StaticResource AccentPrimary}"
                                       VerticalAlignment="Center"/>
                            <!-- # 曲目编号 -->
                            <TextBlock Grid.Column="1"
                                       Text="{Binding TrackNumber, FallbackValue=''}"
                                       FontSize="11"
                                       Foreground="{StaticResource ForegroundSecondary}"
                                       HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            <TextBlock x:Name="PART_Title" Grid.Column="2" Text="{Binding Title}"
                                       FontSize="12" TextTrimming="CharacterEllipsis"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="3" Text="{Binding Artist}"
                                       FontSize="12" TextTrimming="CharacterEllipsis"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource ForegroundSecondary}"
                                       VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="4" Text="{Binding Album}"
                                       FontSize="12" TextTrimming="CharacterEllipsis"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource ForegroundSecondary}"
                                       VerticalAlignment="Center"/>
                            <!-- 时长列 -->
                            <TextBlock Grid.Column="5"
                                       Text="{Binding Duration, Converter={StaticResource TimeSpanToString}}"
                                       FontSize="11" HorizontalAlignment="Right"
                                       Foreground="{StaticResource ForegroundSecondary}"
                                       VerticalAlignment="Center"/>
                            <!-- 删除按钮：悬停变红 -->
                            <Button Grid.Column="6" Content="×"
                                    Width="20" Height="20" Padding="0"
                                    FontSize="14"
                                    Tag="{Binding}"
                                    Click="RemoveButton_Click"
                                    ToolTip="移除">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                        <Setter Property="Foreground" Value="{StaticResource ForegroundSecondary}"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource DangerHover}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
    </Border>
</UserControl>
