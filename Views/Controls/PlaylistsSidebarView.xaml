<!--
    PlaylistsSidebarView —— 左侧歌单栏（Phase 16: 矢量图标 + 选中态背景色）。
    布局: 2 行 Grid
      Row 0 (Auto) : + / − 工具栏（矢量 Path）
      Row 1 (*)    : ListBox 显示歌单，每项含 ▶ marker + 图标 + 名称
-->
<UserControl x:Class="DPlayer.Views.Controls.PlaylistsSidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DPlayer.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:PlaylistsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Background="{StaticResource BackgroundSecondary}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部 + / − 工具栏 -->
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    Margin="6">
            <Button x:Name="AddBtn"
                    Width="28" Height="28"
                    Padding="0"
                    Foreground="{StaticResource ForegroundPrimary}"
                    ToolTip="新建歌单"
                    Click="AddBtn_Click">
                <Path Style="{StaticResource IconPath}" Width="14" Height="14"
                      Data="{StaticResource Icon.Plus}" Stroke="{StaticResource ForegroundPrimary}"/>
            </Button>
            <Button x:Name="RemoveBtn"
                    Width="28" Height="28"
                    Padding="0"
                    Margin="6,0,0,0"
                    Foreground="{StaticResource ForegroundPrimary}"
                    ToolTip="删除选中歌单"
                    Click="RemoveBtn_Click">
                <Path Style="{StaticResource IconPath}" Width="14" Height="14"
                      Data="{StaticResource Icon.Minus}" Stroke="{StaticResource ForegroundPrimary}"/>
            </Button>
        </StackPanel>

        <!-- 歌单列表（Phase 9: 支持拖拽重排，Phase 16: 矢量图标） -->
        <ListBox x:Name="PlaylistList"
                 Grid.Row="1"
                 ItemsSource="{Binding Playlists}"
                 SelectedItem="{Binding ViewedPlaylist, Mode=TwoWay}"
                 BorderThickness="0"
                 Background="Transparent"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 AllowDrop="True"
                 MouseDoubleClick="PlaylistList_MouseDoubleClick"
                 PreviewMouseLeftButtonDown="PlaylistList_PreviewMouseLeftButtonDown"
                 PreviewMouseMove="PlaylistList_PreviewMouseMove"
                 PreviewMouseLeftButtonUp="PlaylistList_PreviewMouseLeftButtonUp"
                 DragOver="PlaylistList_DragOver"
                 DragLeave="PlaylistList_DragLeave"
                 Drop="PlaylistList_Drop">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="bd" Background="Transparent" Padding="6,4" CornerRadius="4">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background"
                                                Value="{StaticResource BackgroundTertiary}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="bd" Property="Background"
                                                Value="{StaticResource AccentPressed}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="14"/>   <!-- ▶ marker -->
                            <ColumnDefinition Width="20"/>   <!-- icon -->
                            <ColumnDefinition Width="*"/>    <!-- Name -->
                        </Grid.ColumnDefinitions>
                        <!-- ▶ 活跃标记由 code-behind RefreshActiveMarker 切换 Visibility -->
                        <Path x:Name="PART_SidebarMarker"
                              Grid.Column="0"
                              Data="{StaticResource Icon.PlayMarker}" Fill="{StaticResource AccentPrimary}"
                              Width="9" Height="9" Stretch="Uniform" Visibility="Collapsed"
                              VerticalAlignment="Center"/>
                        <!-- 图标（手动歌单 🎵，文件夹歌单 Folder，扫描中 Refresh） -->
                        <Grid Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock FontSize="12" Foreground="{StaticResource ForegroundSecondary}" Text="🎵">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasSourceFolder}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Path Data="{StaticResource Icon.Folder}" Stroke="{StaticResource ForegroundSecondary}"
                                  Width="14" Height="14">
                                <Path.Style>
                                    <Style TargetType="Path" BasedOn="{StaticResource IconPath}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasSourceFolder}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsScanning}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Path.Style>
                            </Path>
                            <Path Data="{StaticResource Icon.Refresh}" Stroke="{StaticResource ForegroundSecondary}"
                                  Width="12" Height="12">
                                <Path.Style>
                                    <Style TargetType="Path" BasedOn="{StaticResource IconPath}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsScanning}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Path.Style>
                            </Path>
                        </Grid>
                        <TextBlock Grid.Column="2"
                                   Text="{Binding Name}"
                                   Foreground="{StaticResource ForegroundPrimary}"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
